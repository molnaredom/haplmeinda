name: 🚀 Deploy to haplmelinda.hu

on:
  push:
    branches:
      - master
    paths:
      - "introducing_page/**"
  workflow_dispatch: # Kézi indítás is lehetséges

jobs:
  deploy:
    name: 📦 FTP Deploy
    runs-on: ubuntu-latest

    steps:
      - name: 🔄 Checkout repository
        uses: actions/checkout@v4

      - name: 📦 Install lftp
        run: sudo apt-get install -y lftp

      - name: 📤 Deploy via FTP
        env:
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
        run: |
          cd introducing_page
          lftp -c "
          set ftp:ssl-allow no;
          set ftp:passive-mode yes;
          set net:timeout 30;
          set net:max-retries 3;
          open -u c40067adam,$FTP_PASSWORD wh14.rackhost.hu;
          cd web;
          mirror --reverse --verbose --only-newer --ignore-time --exclude .git/ --exclude node_modules/ --exclude netlify/ --exclude-glob *.md . .;
          glob chmod 644 *;
          glob chmod 644 fenykepek/*;
          chmod 755 .;
          chmod 755 fenykepek;
          quit;
          "

      - name: ✅ Deployment complete
        run: echo "🎉 Successfully deployed to haplmelinda.hu!"
